package org.fairgenomes.transformer.datastructures;

public enum ValueType {
    String, Text, UniqueID, LookupOne, LookupMany, Integer, ReferenceOne, ReferenceMany, Date, DateTime, Boolean, Decimal;
}
